# 🚗 TopTopGo — Site Vitrine

## Déploiement sur Railway

```bash
# 1. Pousser sur GitHub
git init
git add .
git commit -m "TopTopGo site vitrine"
git remote add origin https://github.com/VOTRE_COMPTE/toptopgo.git
git push -u origin main

# 2. Sur railway.app
# → New Project → Deploy from GitHub → sélectionner le repo
# Railway détecte Node.js et lance "npm start" automatiquement ✅
```

C'est tout ! Pas de base de données nécessaire.
